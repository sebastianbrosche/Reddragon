

## 2026-05-25 03:49 - Bot Fleet Expanded to 15, Ticks Accelerated to 5s

**User requested:** "More bots 5 seconds ticks."

**Actions taken:**
1. Launched 10 additional bots via `create_bot()` directly (5 new + 5 more = total 15)
2. Updated `typeclasses/scripts/bot_tick.py`:
   - Class attribute `interval = 5` (was 15)
   - `at_script_creation()` sets `self.interval = 5`
   - `start_bot_tick()` passes `interval=5` to `create_script()`
3. Restarted tick script — confirmed `db_interval=5`, `db_is_active=True`

**Bot Fleet (15 total):**
- Eta: 5 rooms | 125 XP
- Theta: 5 rooms | 125 XP  
- Iota: 5 rooms | 125 XP
- Kappa: 6 rooms | 150 XP
- Lambda: 6 rooms | 150 XP
- Mu, Nu, Xi, Omicron, Pi, Rho, Sigma, Tau, Upsilon, Phi: all at 2 rooms | 50 XP (just spawned)

**Rate:** 3 bot moves per second (15 bots × 5s tick = 3 moves/sec)

## Red Dragon MUD - Self-Hosting on Windows (2026-05-25 ~04:30 GMT+8)

Sebastian wants to self-host Red Dragon MUD on his Windows 11 machine instead of the Alibaba Cloud server (port access blocked, no Alibaba account ownership).

### What We Did
- Created `SETUP_WINDOWS.bat` and `WINDOWS_README.md` in workspace
- Deployed download page to Cloudflare Pages: https://74e032cb.rcp-housing.pages.dev/reddragon-download.html
- Download ZIP: https://74e032cb.rcp-housing.pages.dev/reddragon-windows.zip

### Sebastian's Install
- Has Python 3.11 installed (not 3.12, despite what he thought)
- Evennia `pip install` succeeded
- `evennia` command not in PATH → used `python -m evennia` instead
- Database migration completed successfully
- Created superuser account
- Server running on localhost:3000 (telnet) and localhost:8000 (web)

### Fixes Applied During Session
1. **Connection screen missing** → Added `CONNECTION_SCREEN` string to `commands/unloggedin.py`
2. **Password validation blocking simple passwords** → Added `AUTH_PASSWORD_VALIDATORS = []` to `settings.py`

### Web Client
- Standalone HTML client deployed at https://40ca3275.rcp-housing.pages.dev/reddragon-client.html
- For local server: need to update WS URL to `ws://localhost:4001`
- Evennia WebSocket protocol format: `["text", [cmd], {}]`

### Next Steps / Open Questions
- Sebastian wants to give me SSH access via ngrok + OpenSSH so I can edit files remotely
- I warned him about security risks of exposing his machine
- Alternative: keep PowerShell open and paste commands I give him
- Need to decide on remote access approach before making more code changes

### Key Files on Sebastian's Machine
- `C:\reddragon\server\conf\settings.py` - server config
- `C:\reddragon\commands\unloggedin.py` - login screen
- `C:\reddragon\` - full Evennia game directory

### Server Status (as of session end)
- Evennia running: Portal on 3000/8000/4001, Server on 8001/5000
- Can connect via web client at http://localhost:8000
- Can connect via MUD client at localhost:3000
